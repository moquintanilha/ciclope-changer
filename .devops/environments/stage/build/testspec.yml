---
version: 0.2
phases:
  install: 
    on-failure: ABORT
    commands:
      - echo "Start functional tests."
      - npm cache clean --force
      - npm cache verify
      - npm install
      - npm test
